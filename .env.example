# 서버 포트 (휴대폰에서 접속하려면 PC와 같은 Wi-Fi에서 http://<PC-IP>:PORT)
PORT=5173

# 선택: Claude API 키. 설정하면 자연스러운 한국어 해석 + 핵심 단어/표현 해설을 Claude가 생성합니다.
# 비워 두면 무료 번역(Google 번역 비공식 엔드포인트) + 사전 API 기반의 기본 모드로 동작합니다.
ANTHROPIC_API_KEY=
